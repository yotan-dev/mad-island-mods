using UnityEngine;

namespace HFramework.SexScripts.ScriptContext
{
	/// Marks a string field as a context tag selector.
	/// The field will display a dropdown with known tags from the registry.
	public class ContextTagAttribute : PropertyAttribute
	{
	}
}
